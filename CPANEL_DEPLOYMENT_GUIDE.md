# دليل رفع الموقع على cPanel

## ✅ الموقع جاهز للرفع!

تم بناء الموقع بنجاح وجميع الملفات جاهزة في مجلد `out`.

---

## 📦 الخطوات المطلوبة للرفع:

### 1. ✅ البناء (تم بالفعل)
الموقع تم بناؤه بنجاح باستخدام:
```bash
npm run build
```

مجلد `out` يحتوي الآن على جميع الملفات الثابتة الجاهزة للرفع.

### 2. 📤 رفع الملفات إلى cPanel

#### الطريقة الأولى: File Manager (الأسهل)
1. افتح **cPanel** الخاص بك
2. اذهب إلى **File Manager**
3. انتقل إلى مجلد `public_html`
4. احذف أي ملفات قديمة (إذا كان هناك موقع قديم)
5. ارفع **جميع محتويات** مجلد `out`:
   - `.htaccess`
   - `index.html`
   - مجلد `_next`
   - مجلد `ar`
   - مجلد `en`
   - مجلد `images`
   - مجلد `404`
   - ملف `404.html`

**⚠️ مهم جداً:** ارفع **محتويات** مجلد `out` وليس المجلد نفسه!

#### الطريقة الثانية: FTP
1. استخدم برنامج FTP مثل FileZilla
2. اتصل بالسيرفر باستخدام بيانات FTP من cPanel
3. انتقل إلى مجلد `public_html`
4. ارفع جميع محتويات مجلد `out`

### 3. ✅ ملف .htaccess (تم تضمينه)
ملف `.htaccess` موجود بالفعل في مجلد `out` وسيتم رفعه تلقائياً.

**وظيفة الملف:**
- إعادة توجيه جميع الطلبات إلى `index.html`
- تفعيل الضغط (Compression)
- تفعيل التخزين المؤقت (Caching)
- دعم HTTPS (اختياري)

### 4. 🔍 التحقق من الرفع
بعد رفع الملفات، تأكد من:
- ✅ جميع المجلدات موجودة في `public_html`
- ✅ ملف `.htaccess` موجود (قد يكون مخفياً، فعّل "Show Hidden Files")
- ✅ ملف `index.html` موجود في المجلد الرئيسي
- ✅ صلاحيات الملفات: 644 للملفات، 755 للمجلدات

### 5. 🧪 اختبار الموقع
افتح موقعك في المتصفح وتحقق من:
- ✅ الصفحة الرئيسية تفتح بشكل صحيح
- ✅ التنقل بين الصفحات يعمل
- ✅ تبديل اللغة (العربية/الإنجليزية) يعمل
- ✅ الصور والأيقونات تظهر
- ✅ صفحات الخدمات والمشاريع والمدونة تعمل
- ✅ نموذج الاتصال يعمل

---

## 📋 معلومات تقنية:

### بنية الموقع:
```
public_html/
├── .htaccess          (إعدادات Apache)
├── index.html         (إعادة توجيه للغة الافتراضية)
├── 404.html           (صفحة الخطأ)
├── _next/             (ملفات Next.js)
├── ar/                (النسخة العربية)
│   ├── index.html
│   ├── about/
│   ├── services/
│   ├── projects/
│   ├── blog/
│   └── contact/
├── en/                (النسخة الإنجليزية)
│   ├── index.html
│   ├── about/
│   ├── services/
│   ├── projects/
│   ├── blog/
│   └── contact/
└── images/            (الصور)
```

### مميزات الموقع:
- ✅ **Static Export**: موقع ثابت بالكامل
- ✅ **لا يحتاج Node.js**: يعمل على أي استضافة
- ✅ **سريع جداً**: جميع الصفحات مُنشأة مسبقاً
- ✅ **SEO Friendly**: محسّن لمحركات البحث
- ✅ **متعدد اللغات**: عربي وإنجليزي
- ✅ **Responsive**: متجاوب مع جميع الأجهزة

---

## 🔧 حل المشاكل الشائعة:

### المشكلة: الصفحة الرئيسية تعمل لكن الصفحات الأخرى تعطي 404
**الحل:**
1. تأكد من وجود ملف `.htaccess` في `public_html`
2. تأكد من تفعيل `mod_rewrite` في Apache (اتصل بالدعم الفني)
3. تحقق من صلاحيات ملف `.htaccess` (يجب أن تكون 644)

### المشكلة: ملف .htaccess غير موجود
**الحل:**
1. في File Manager، فعّل "Show Hidden Files" من Settings
2. إذا لم يظهر، ارفعه يدوياً من مجلد `out`

### المشكلة: الصور لا تظهر
**الحل:**
1. تأكد من رفع مجلد `images` بالكامل
2. تحقق من صلاحيات مجلد `images` (755)
3. تأكد من رفع مجلد `_next` الذي يحتوي على الأصول

### المشكلة: اللغة العربية لا تعمل
**الحل:**
1. تأكد من رفع مجلد `ar` بالكامل
2. تحقق من وجود ملف `index.html` داخل `ar/`

---

## 📞 الدعم الفني:

إذا واجهت أي مشاكل:
1. تحقق من لوحة التحكم في cPanel للأخطاء
2. راجع Error Logs في cPanel
3. اتصل بالدعم الفني للاستضافة للتأكد من:
   - تفعيل `mod_rewrite`
   - دعم `.htaccess`
   - صلاحيات الملفات

---

## 🎉 بعد الرفع الناجح:

موقعك الآن جاهز ويعمل! يمكنك:
- ✅ مشاركة الرابط مع العملاء
- ✅ ربط Domain مخصص
- ✅ تفعيل SSL Certificate (HTTPS)
- ✅ إضافة Google Analytics
- ✅ إعداد البريد الإلكتروني للنماذج
